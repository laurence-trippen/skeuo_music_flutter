import 'package:flutter/material.dart';

class InterpretsScreen extends StatelessWidget {
  const InterpretsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Interprets"), color: Colors.cyan, alignment: Alignment.center,);
  }
}
